/* Imposta la tipografia con il font Inter */
body {
  font-family: 'Inter', sans-serif;
}

/* Classe per il fade-in all'avvio */
.fade-in {
  animation: fadeIn 0.5s ease-in-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
